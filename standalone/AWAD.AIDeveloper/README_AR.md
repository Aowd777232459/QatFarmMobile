# AWAD AI Developer 1.1

تطبيق مستقل من عواد سوفت لتحويل الدردشة إلى دورة تطوير برمجية فعلية وآمنة على جهاز المستخدم.

## الجديد في 1.1

- **محرر ملفات داخلي**: اضغط على أي ملف في شجرة المشروع لفتحه وتعديله، مع إنشاء checkpoint ZIP قبل الحفظ.
- **معاينة Diff قبل التنفيذ**: يعرض الفرق المتوقع لكل ملف قبل اعتماد خطة الذكاء الاصطناعي.
- **مراجع ذكاء اصطناعي مستقل**: يفحص الخطة من ناحية الصحة، النطاق، الانحدارات، الأمان، فقدان البيانات، واختبارات البناء، ويعطي درجة من 100.
- **Terminal آمن داخل المشروع**: تشغيل أوامر التطوير المسموحة فقط، بدون shell chaining أو إعادة توجيه.
- **لوحة Git**: تعرض الفرع الحالي، حالة الملفات، ملخص diff وآخر commits.
- **Git Checkpoint يدوي**: ينفذ `git add -A` ثم commit محلي بعد موافقة المستخدم، ولا ينفذ push.
- واجهة مطور محسنة مع أدوات Git وTerminal والمحرر ومعاينة الفرق.

## القدرات الأساسية

- فتح أي مشروع محلي على الكمبيوتر وتحليل بنية الملفات.
- محادثة عربية مع نموذج ذكاء اصطناعي عبر OpenAI Responses API.
- إنشاء خطة تعديلات قابلة للتدقيق قبل لمس الملفات.
- كتابة ملفات جديدة، استبدال نصوص محددة، حذف ملفات، وتشغيل أوامر Build/Test مسموحة.
- إنشاء نقطة استعادة ZIP تلقائياً قبل كل عملية تطبيق.
- وضع **التطوير التلقائي**: يطبق الخطة، يشغل البناء/الاختبار، ثم يعيد إرسال الأخطاء للنموذج لدورة إصلاح جديدة حتى 5 دورات.
- ذاكرة محلية لكل مشروع تحفظ ملخص المهام والنتائج السابقة.
- **نسخة تطوير آمنة Staging**: ينسخ المشروع إلى مجلد منفصل ثم يعمل على النسخة بدلاً من الأصل.
- حماية من الكتابة خارج مجلد المشروع ومن تعديل `.git` أو ملفات الأسرار والمفاتيح.
- قائمة أوامر تطوير مسموحة بدل تنفيذ shell مفتوح.

## التطوير الذاتي الآمن

لا يعدّل البرنامج الملف التنفيذي الذي يعمل منه مباشرة. لتطوير AWAD AI Developer نفسه، افتح مجلد مصدره ثم اضغط **إنشاء نسخة تطوير آمنة**. بعدها اطلب من الدردشة تعديل النسخة المرحلية، بنائها واختبارها. بعد نجاحها يمكن اعتماد الإصدار الجديد يدوياً.

## التشغيل من المصدر

يتطلب .NET 10 SDK:

```powershell
cd AWAD.AIDeveloper
dotnet run
```

يفتح التطبيق محلياً على:

`http://127.0.0.1:45873`

## مفتاح الذكاء الاصطناعي

الأفضل تعريفه كمتغير بيئة في Windows:

```powershell
setx OPENAI_API_KEY "YOUR_KEY"
```

ثم أعد فتح التطبيق. ويمكن إدخاله في شاشة الإعدادات للجلسة الحالية فقط. التطبيق لا يكتب المفتاح داخل المشروع.

النموذج الافتراضي هو `gpt-5.6-sol` ويمكن تغييره من الإعدادات.

## حدود الأمان

الأوامر تبدأ فقط بواحد من الأدوات التالية:

`dotnet, npm, npx, node, git, python, python3, py, pytest, cargo, go, mvn, mvnw, gradle, gradlew, java`

لا يسمح بمسارات مطلقة للكتابة، ولا بالكتابة خارج مجلد المشروع، ولا بتعديل ملفات المفاتيح أو الأسرار، ولا بالأوامر الإدارية أو النظامية الخطرة. Terminal المدمج يمنع كذلك رموز ربط shell مثل `& | > <`.

## الهيكل

1. واجهة دردشة عربية.
2. Project Context Builder لاختيار الملفات الأقرب للمهمة.
3. Planner Agent لتحويل الطلب إلى JSON Plan.
4. Reviewer Agent لمراجعة الخطة بشكل مستقل.
5. Safety Gate للتحقق من المسارات والأوامر.
6. Diff Preview لمعاينة ما سيتغير قبل التنفيذ.
7. Checkpoint Engine لنسخة ZIP قبل التنفيذ أو الحفظ اليدوي.
8. Executor لتطبيق التغييرات داخل Workspace فقط.
9. Verifier لتشغيل Build/Test.
10. Repair Loop لإعادة أخطاء البناء للنموذج.
11. Project Memory لحفظ نتائج المهام السابقة.
12. File Editor + Safe Terminal + Git Console.

## المرحلة التالية

- Git branch تلقائي لكل مهمة بدل العمل على الفرع الحالي.
- وكيل اختبارات مستقل ينشئ اختبارات ويشغلها بعد التنفيذ.
- سجل إصدارات SQLite مع إمكانية Rollback من الواجهة.
- دعم أكثر من مزود ذكاء اصطناعي.
- Function Calling بدل بروتوكول JSON النصي.
- مدير Plugins/Tools لإضافة قدرات جديدة بدون تغيير النواة.
- Update Installer يبني الإصدار الذاتي الجديد ثم يعرض زر اعتماد/استرجاع.
